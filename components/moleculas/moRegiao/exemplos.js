
import React from 'react';
import Rota from './index.js';

export const moRegiao = (modo) => (
    <div>
      <h2>moRegiao</h2>
        <Rota modo={modo} regiao="RJ"/>
    </div>
);

export default moRegiao;
